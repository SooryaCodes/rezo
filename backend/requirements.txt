fastapi>=0.115
uvicorn[standard]>=0.30
# Every provider is called over raw HTTP in app/llm/client.py, so httpx is a
# direct dependency, not one inherited from a vendor SDK.
httpx>=0.27
langgraph>=0.2
langchain-core>=0.3
sqlalchemy>=2.0
psycopg2-binary>=2.9
python-multipart>=0.0.9
pillow>=10.0
websockets>=12.0
# chromadb>=0.5  # only needed for REZO_RETRIEVER=chroma; imported lazily

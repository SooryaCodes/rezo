# Rezo Frontend

Next.js application serving three surfaces:

- `/store/[id]` — buyer-facing storefront with the dispute chat and live evidence capture flow
- `/dashboard` — seller console: policy wizard, dispute inbox, approval dossiers, analytics
- `/console` — real-time agent monitoring console (React Flow graph + event stream)

Scaffolding lands with the Round 2 prototype build.
